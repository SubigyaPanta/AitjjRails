<%= form_for(ps_two_quotation) do |f| %>
  <% if ps_two_quotation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(ps_two_quotation.errors.count, "error") %> prohibited this ps_two_quotation from being saved:</h2>

      <ul>
      <% ps_two_quotation.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ps_two_authors_id, "Select Author" %>
    <%#= f.text_field :ps_two_authors_id %>
    <%#= f.collection_select(:ps_two_authors_id, PsTwo::Author.all + [PsTwo::Author.new(id: -1, name: "New Author")], :id, :name, {:selected =>'-1'}) %>
    <%= f.collection_select(:ps_two_authors_id, PsTwo::Author.all + [PsTwo::Author.new(id: -1, name: "New Author")], :id, :name) %>
    <%= text_field_tag :new_author %>
  </div>

  <div class="field">
    <%= f.label :ps_two_categories_id, "Select Category" %>
    <%#= f.text_field :ps_two_categories_id %>
    <%#= f.collection_select(:ps_two_categories_id, PsTwo::Category.all + [PsTwo::Category.new(id: -1, name: "New Category")], :id, :name, {:selected => '-1'}) %>
    <%= f.collection_select(:ps_two_categories_id, PsTwo::Category.all + [PsTwo::Category.new(id: -1, name: "New Category")], :id, :name) %>
    <%= text_field_tag :new_category %>
  </div>

  <div class="field">
    <%= f.label :quote %>
    <%= f.text_area :quote %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
