<p id="notice"><%= notice %></p>

<h1>Ps Two Quotations</h1>

<table>
  <thead>
    <tr>
      <th>Ps two authors</th>
      <th>Ps two categories</th>
      <th>Quote</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @ps_two_quotations.each do |ps_two_quotation| %>
      <tr>
        <td><%= ps_two_quotation.ps_two_authors %></td>
        <td><%= ps_two_quotation.ps_two_categories %></td>
        <td><%= ps_two_quotation.quote %></td>
        <td><%= link_to 'Show', ps_two_quotation %></td>
        <td><%= link_to 'Edit', edit_ps_two_quotation_path(ps_two_quotation) %></td>
        <td><%= link_to 'Destroy', ps_two_quotation, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Ps Two Quotation', new_ps_two_quotation_path %>
